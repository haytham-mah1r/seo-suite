name = "edge-seo-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (never put these here — set them with `wrangler secret put NAME`):
#   DATAFORSEO_LOGIN
#   DATAFORSEO_PASSWORD
#   ANTHROPIC_API_KEY   (optional — powers LLM Visibility "auto-check" for Claude
#                         and the Content Grader's "deep review")
#   OPENAI_API_KEY       (optional — powers "auto-check" for ChatGPT)
#   PERPLEXITY_API_KEY   (optional — powers "auto-check" for Perplexity)
